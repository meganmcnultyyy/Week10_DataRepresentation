import React from "react";

export class Header extends React.Component { // Header Class 
    render() {
        return (
            <div>
                <h1>My Header in another component</h1>
            </div>
        )
    }
}